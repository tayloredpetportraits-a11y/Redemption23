# Supabase Configuration
# You can find these values in your Supabase Dashboard: Project Settings -> API
NEXT_PUBLIC_SUPABASE_URL="https://your-project-id.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-supabase-anon-key"

# Supabase Service Role Key (Critical for Admin functions)
# Found in Supabase Dashboard: Project Settings -> API -> Service Role Secret
# WARNING: Keep this secret! Do not expose it to the client.
SUPABASE_SERVICE_ROLE_KEY="your-supabase-service-role-key"

# Admin Authentication
# The password required to log in to the /admin routes
ADMIN_PASSWORD="your-secure-admin-password"

# JWT Secret
# Used to sign and verify JSON Web Tokens for secure admin sessions.
# You can generate a strong secret using a tool like `openssl rand -hex 32` or an online generator.
JWT_SECRET="your-random-verification-string-at-least-32-chars"

# Stripe Configuration
# Found in Stripe Dashboard: Developers -> API Keys
STRIPE_SECRET_KEY="sk_test_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# Stripe Webhook Secret
# Found in Stripe Dashboard: Developers -> Webhooks -> [Select Endpoint] -> Signing Secret
# Required to verify that events sent to your webhook endpoint are genuinely from Stripe.
STRIPE_WEBHOOK_SECRET="whsec_..."

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000
